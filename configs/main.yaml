run:
  out_dir: runs
  seed: 42
  device: cuda
  gpu_id: 0
  num_workers: 0
  wandb: false              # log to Weights & Biases (turn it on in the run config)
  wandb_project: default-project
  wandb_watch: true
  wandb_watch_log: all
  wandb_watch_freq: 100

data:
  path: ~
  user_col: user_id
  item_col: item_id
  time_col: timestamp
  sep: "\t"
  min_user_inter: 5
  min_item_inter: 5

split:
  strategy: leave_one_out   # leave_one_out | temporal | random
  val_ratio: ~
  test_ratio: ~

model:
  params: {}
  batch_size: 512

eval:
  ks:
    - 5
    - 10
    - 20
  metrics:
    - recall
    - ndcg
    - hit
    - mrr
  batch_size: 512
  seen_mode: except_target   # all | except_target | none: which already seen items are masked from the ranking
  monitor: ndcg@10
  val_every: 1
  patience: 0
  save_recs: true

features:
  path: ~
  id_col: ~
  cols: ~
  drop_cols: []
  sep: "\t"
  normalize: standardize

quantizer:
  params: {}
  dedup: true